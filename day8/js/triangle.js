document.addEventListener("click", function() {
  var base = prompt("enter the length of base: ")/1;
  var height = prompt("enter the height: ")/1;
  var area = base * height / 2;
  alert("area: " + area);
})
